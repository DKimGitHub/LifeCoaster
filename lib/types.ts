export type PageProps = {
    params?: any;
    children?: React.ReactNode;
  };

  export type dataType = {
    [key: string]: any;
  }
  export type FormState = {
    xValue: number;
    yValue: number;
  }